<a class="clear-notf" style="float: right;
        position: relative;
        display: flex;
        color: black !important;
        padding: 0.5rem !important;" href="{{ route('admin.notifications-clear', $notification->id) }}" class="text-right">{{ __("Clear") }}</a>